[project]
name = "cicdtest"
version = "0.1.0"
description = "Python 3.12 app managed by uv"
requires-python = ">=3.12"
dependencies = [
    "pytest>=8.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
